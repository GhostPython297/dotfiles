alias e="exit"
alias pr="fish --private"
alias dpkg="sudo dpkg -i"
alias dl="yt-dlp"
alias df="duf"
alias f="fzf"
alias cat="batcat"
alias bg="~/./.fehbg"
#alias cava="TERM=screen-256color cava"

# nvim
alias vi="~/./AppImages/neovim.appimage"
alias i="~/./AppImages/neovim.appimage"

# git
alias gs="git status"
alias gc="git commit"
alias gl="git log"

# exa
# icons: alias l="exa --classify --color=always --icons --all --sort=type" # lista tradicional
alias l="exa --classify --color=always --all --sort=type --icons" # lista tradicional
alias ls="exa --classify --color=always --all --sort=type --icons" # lista tradicional
alias lt="exa --classify --color=always --all --sort=type --tree --level=2 --icons" # lista em árvore
alias lg="exa --classify --color=always --all --sort=type --long --header --links --no-user --icons" # lista em tabela

# vi keybindigs
# fish_vi_key_bindings
# set fish_cursor_default block blink
# set fish_cursor_insert line blink
# set fish_cursor_visual underscore
